# Job Application Service Client Questionnaire

Static HTML questionnaire for collecting client details and exporting a Word document locally in the browser.

This page does not include a backend submission endpoint. Responses stay in the browser until the user exports the `.docx` file.
